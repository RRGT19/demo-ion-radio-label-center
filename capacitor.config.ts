import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'demo-ion-radio-label-center',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
